// @ts-check
/* eslint-env node */

const fromCode = require('./from-code')
const fromFile = require('./from-file')
const renderer = require('./renderer')

module.exports = { renderer, fromCode, fromFile }
